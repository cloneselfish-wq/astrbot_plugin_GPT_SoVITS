# 更新日志

## v3.6.3

### 变更

- `_aiocqhttp_self_id()` 改为**优先取 `message_obj.self_id`**（与 AstrBot 自身在
  `aiocqhttp_message_event` 里的写法一致），`raw_message["self_id"]` 仅作兜底。
  两处都没有时返回 `None`，即不向 OneBot 传 `self_id` 路由参数。

## v3.6.2

### 修复

- **外语语音后的中文原文在群里看不见**：之前是把 `[语音][文字]` 塞进**同一条消息**发出去。
  实测 NapCat 侧确实收到了 text 段（`发送 -> 群聊 … [语音 8s] 原文`），但 QQ 客户端
  对「语音 + 文字」混排只渲染语音，文字被丢掉了，群里只听到语音、看不到原文。

### 变更

- 改成**两条消息**：语音先单独发出去拿到 `message_id`，再发一条
  **引用该语音 + 中文原文**的消息。引用由 AstrBot 正常发送管线发出
  （`Reply` 组件），保留分段、限速与发送后钩子。
- 为此新增 `_send_voice_direct()`：`AiocqhttpMessageEvent._dispatch_send` 把
  `send_group_msg` 的返回值丢掉了，要引用自己刚发的语音只能绕开它直接调底层
  OneBot 接口拿 id（私聊走 `send_private_msg`）。
- 拿不到 id 时（非 aiocqhttp 平台 / 发送失败）**自动退回原来的混排方式**，原文不会丢。

## v3.6.1

### 修复

- **`gsv_tts` 工具链路漏了「外语语音附中文原文」**：v3.6.0 只把「附中文原文」做在了发送前钩子里，
  模型主动调用 `gsv_tts` 直接发语音时（群友说「让沙绫发语音」很可能走这条）不会附，
  于是群里只听到一段日语、看不到原文。现在两条链路共用同一份判断，行为一致。

### 变更

- 把「语音后面要不要再跟一份文字」的判断抽成 `_keep_text_after_speech()`，
  发送前钩子与 `gsv_tts` 工具共用，避免以后只改一处又出现行为不一致。
- `gsv_tts` 工具返回值在附了原文时改为「语音已发送（语音后已附上文字原文）…」，
  让模型知道文字已经带上了、不必再补。

## v3.6.0

### 新增

- **群友点名要语音时必须出声**：以前「想让机器人说话」只靠两件事——`auto.tts_prob` 的低概率抽签，
  以及模型自己想起来调用 `gsv_tts` 工具。可实际上群友说「说句话」时，模型会把这句读成普通聊天，
  直接回文字（在群聊的真实上下文里，`glm-5.3-flash` 和 `deepseek-flash` 都不调工具），
  于是出现「明明点名了却不吭声」。现在插件自己识别这类请求，命中就**绕过概率**直接转语音：

  - 命中「说句话 / 说一句 / 说点什么 / 用语音说 / 发条语音 / 用你的声音 / 念一下 / 念给我听 /
    说给我听 / 听你说 / 唱一句 / 出个声」等说法；
  - 否定式优先，「别用语音 / 文字就好 / 别出声」不会触发；
  - 只在 30 字以内的短消息里识别，长文里提到「语音」不算要求。

- **外语语音后面自动附中文原文**：日语音色（开了「合成前翻译」）说出来的语音，群友听不出里面的中文内容。
  现在这类语音后面会自动跟上送合成前的那份中文原文，方便对照。只在「语音语言不是中文」**且**
  「原文本身是中文」时才附——上面点名改说中文的语音不会重复发文字，人格本来就回日语时也不会硬凑。
  与 `auto.dual_output` 不叠加（那个已经会附全文）。

- 新增 `auto.voice_on_request`（默认开启）与 `auto.zh_text_on_foreign_voice`（默认开启）两项配置。

### 改进

- 给「回复转语音」链路上原本静默的几处 `return` 补上 DEBUG 日志（概率未命中、不是 LLM 结果、
  没有可朗读的文本），排查「为什么没发语音」时能看到到底卡在哪一步。

## v3.5.0

### 新增

- **语音语言点播**：开了「合成前翻译」的外语音色（日语音色等）不再恒定说外语。
  群友说「说中文」「用中文说」「换成中文」「说人话」「听不懂日语」这类要求时，语音临时改用中文：

  - 明确的请求会在本会话记住 `translate.switch_ttl_minutes` 分钟（默认 10），期间不必反复强调；
  - 模糊表达（如只发「用中文」）只影响被点名的那一次，避免误判后整个会话跟着变；
  - 「别说中文 / 别说日语」这类否定式也能正确识别；
  - 超过 60 字的长消息不参与识别，避免长文里提到「中文」被误判；
  - 新增 `语音语言 <中文|日语|英语|韩语|默认>` 指令，可手动切换与恢复；
  - `gsv_tts` 工具新增 `lang` 参数，用户明确要求换语言时模型会自己带上。

- 新增 `translate.allow_switch`（默认开启）与 `translate.switch_ttl_minutes`（默认 10）两项配置。

### 说明

- 只对**开启了 `auto_translate`** 的音色生效：中文音色换语言只会得到怪口音，不参与点播。
- 关闭 `allow_switch` 即回到「恒定说 `text_lang`」的旧行为。

## v3.4.0

### 新增

- **LLM 主动调用语音**：重写 `gsv_tts` 工具的描述，明确告诉模型「什么时候该用语音说话」
  （用户说「用语音说 / 语音回复我 / 念一下 / 唱一句」、需要靠声音表达情绪、或模型自己觉得
  这句话说出来更好），普通聊天则不应调用。同时给工具补上护栏：
  - 内容超长（超过 `auto.max_msg_len`）直接拒绝并提示模型精简或改用文字；
  - 空内容、未配置音色的机器人直接拒绝；
  - 合成失败 / 服务不可达时返回明确指引，让模型改用文字回复，**不会重复调用**；
  - 工具成功发过语音后打标记，发送前钩子会跳过这一轮的自动转语音，避免同一条回复发两段语音。
- 新增 `auto.only_configured_bots`：打开后，**没有命中「音色档案」的机器人不再转语音**，
  直接发文字。此前它们会回退到全局配置（客户端地址 + 模型配置 + TTS 默认参数），
  等于用别的角色的音色开口说话。默认关闭以保持向后兼容；一条档案都没配置时本项不生效。

### 修复

- **本机 TTS 没启动时会白等**：客户端原来只设了 `total` 超时，服务没监听时也要耗满整个
  `timeout`（可能 180 秒）才能判定失败。现在单独限制建连阶段（`connect` / `sock_connect` 各 5 秒），
  并加入**冷却熔断**：连不上后 60 秒内直接判定不可用、不再发起请求，期间消息照常发文字。
  服务恢复后自动解除。
- 客户端区分「服务不可达」与「服务在但合成报错」（`GSVRequestResult.unreachable`），
  前者降为 warning 日志并静默退回文字，后者仍报 error。
- `/说` 指令与 `gsv_tts` 工具在服务不可用时给出可读的中文提示（原来直接把底层异常抛给用户）。

## v3.3.0

### 新增

- 新增「语音翻译」：音色档案可开启 `auto_translate`，合成前先用 LLM 把机器人回复翻译成该档案「合成文本的语言」（`text_lang`），再由 GPT-SoVITS 合成。
  - 用途：用日语（或其它语言）训练的音色，直接用中文合成会带明显外语口音；翻译后再合成即可让日文音色以母语发音，听起来自然得多。
  - **文字回复本身不受影响**，仍然是原语言（中文），只有送去合成的文本被翻译。
  - 回复若已经就是目标语言则跳过翻译；语言无法判断（纯 emoji / 数字）时也跳过。
  - 翻译失败或超时会自动回退：用原文合成，并把 `text_lang` 改回原文语言，避免口音错乱，语音不会因此丢失。
  - `auto_translate` 仅在档案级别生效，默认关闭。
- 音色档案新增 `translate_style`：追加给翻译模型的口吻提示（例如「活泼的年轻女性口语」），留空使用内置通用设定。
- 新增 `translate` 配置段，可单独指定翻译用的 LLM 提供商（`translate.provider_id`）；留空则沿用「LLM 配置」里的提供商，再留空则使用当前默认提供商。
- 新增 `core/translate.py`：语言启发式判断 `detect_lang()` 与 `VoiceTranslator`。

### 变更

- `说` 指令、自动转语音、`gsv_tts` 工具三条合成路径都会按档案的翻译设置处理文本。

## v3.2.1

### 修复

- 修复「机器人回复自动转语音」几乎不生效的问题：
  - 原逻辑要求消息链中**全部**都是文本片段才处理，机器人回复只要带一张图片、一个 @ 或表情，整条就被跳过。现在改为按片段处理，图片、@、引用等非文本组件原样保留。
  - 合成失败或异常时不再静默丢弃，而是退回发送原文，避免消息丢失。
  - 转成的语音消息会带上原文（`Record.text`），在支持显示语音文本的客户端上可读。

### 变更

- `auto.tts_prob` 默认值由 `0.15` 调整为 `1.0`，即**机器人每一条对话回复都转成语音**；需要保留随机性可自行调低。
- `auto.max_msg_len` 默认值由 `50` 调整为 `150`，滑块上限提高到 `500`。超过该长度的回复仍直接发文字，避免合成耗时过长（本机实测合成耗时约为音频时长的 2 倍）。
- 新增 `auto.dual_output` 配置：打开后同一条回复会先发语音、再发一遍文字；默认关闭（只发语音）。
- 配置项文案改为「回复转语音配置」，明确该功能作用于机器人自身的对话回复。

## v3.2.0

### 新增

- 新增「音色档案」配置 `bot_voices`，可按机器人 QQ 号（`self_id`）分配独立音色：
  - 每个档案可单独指定实例地址 `base_url`、GPT / SoVITS 权重、参考音频与参考语言、语速与片段间隔。
  - `self_id` 支持填多个 QQ 号，用英文逗号分隔。
  - 未命中档案的机器人继续沿用原有全局配置，保持向后兼容。
  - 新增 `emotion_ref_audio` 开关：关闭时（默认）情绪条目只调节语速与停顿，不会覆盖该机器人档案的参考音频；开启后情绪条目自带的参考音频才会生效。
- 新增 `GSVClientPool`，按实例地址复用并管理多个 GPT-SoVITS 客户端。

### 变更

- `GSVApiClient` 构造参数由 `PluginConfig` 改为 `(base_url, timeout)`，以便同时对接多个实例。
- `GPTSoVITSService.load_model()` 启动时按档案逐个加载模型，并对共用同一实例的档案给出告警。
- `说` 指令、自动转语音与 `gsv_tts` 工具调用均会先按 `self_id` 匹配音色档案，再决定实例与参数。
- `重启GSV` 指令会依次重启默认实例与所有音色档案实例。

### 说明

- 一个 GPT-SoVITS 实例同一时间只能常驻一套音色，切换音色需要重载数 GB 权重、耗时长，不适合在会话中热切换。需要多个音色请在部署 GPT-SoVITS 的机器上分别启动多个实例（例如 9880、9881），再在音色档案中指向不同端口。

## v3.1.0

### 新增

- 新增 `LocalDataManager`，统一管理本地音频读写与缓存逻辑。
- 新增缓存配置项：
  - `cache.enabled`：是否启用参数级缓存。
  - `cache.expire_hours`：缓存过期时间（小时，`0` 表示永不过期）。
  - `cache.path`：缓存目录。

### 优化

- TTS 推理流程支持“先查缓存再请求”：
  - 参数一致时直接复用本地音频，跳过重复推理请求。
  - 未命中时请求 GPT-SoVITS，成功后自动落盘供后续复用。
- 音频文件命名改为参数哈希命名：`gsv_<hash>.<ext>`，确保同参数稳定命中同一文件。
- 发送流程优先使用本地文件路径发送语音，无法读取时回退为 Base64 发送。
- `GSVRequestResult` 增加文本与缓存文件路径信息，便于链路透传和调试。
